import { Stack } from 'expo-router';

/** Document Scanner section: the scan + review + library screen (`index`). */
export default function ScanLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
